const Nodecaf = require('nodecaf');
const api = require('./api');

module.exports = () => new Nodecaf({
    <%_ if(locals.conf){ _%>conf: __dirname + '/<%= locals.conf %>',<% } %>

    startup({ global, conf }){
        
    },

    shutdown({ global, conf }){

    },

    api
});
